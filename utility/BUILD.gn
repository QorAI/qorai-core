# Group depending on upstream's counterpart to make it easier to manage
# dependencies in qorai/ (//chrome/utility does not depend on //qorai/utility,
# so //qorai/utility becomes essentially an alias for //chrome/utility now).
group("utility") {
  public_deps = [ "//chrome/utility" ]
}
